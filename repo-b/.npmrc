# Configure pnpm to handle optional postinstall scripts
# Some packages like rollup have optional postinstall scripts that may fail
# This doesn't affect functionality - we can ignore them

# Alternative: users can install with --ignore-scripts and manually run esbuild install
# pnpm install --ignore-scripts && node node_modules/esbuild/install.js

